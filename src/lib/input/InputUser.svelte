<input placeholder="what needs to be done?" on:keydown />

<style>
    input {
		font-family: "Poppins";
		margin-top: 12px;
		padding: 0.5em 1em;
		font-size: 1.4em;
		grid-column: 1/3;
		backdrop-filter: blur(16px) saturate(180%);
		-webkit-backdrop-filter: blur(16px) saturate(180%);
		background-color: var(--neumorphism-bg);
		border-radius: 12px;
		border: 1px solid var(--neumorphism-border-color);
	}
</style>